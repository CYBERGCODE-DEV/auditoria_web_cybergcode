{
  "name": "cybergcode-web-audit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CYBERGCODE Web Audit Intelligence - auditoría web integral compatible con Vercel",
  "scripts": {
    "dev": "npx vercel@latest dev",
    "check": "node --check api/audit.js && node --check api/report.js && node --check lib/audit/engine.js && node --check lib/security/url-guard.js",
    "test": "node --test"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "pdf-lib": "^1.17.1"
  }
}
